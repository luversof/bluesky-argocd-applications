apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seaweedfs-data-pvc
spec:
  volumeName: seaweedfs-data-pv
  storageClassName: ""
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  
