apiVersion: v1
kind: PersistentVolume
metadata:
  name: seaweedfs-data-pv
  labels:
    app: seaweedfs-data-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: {{ .Values.PersistentVolume.hostPath.path }}
