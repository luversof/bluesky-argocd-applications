apiVersion: v1
kind: PersistentVolume
metadata:
  name: superset-home-pv
  labels:
    app: superset-home-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: {{ .Values.PersistentVolume.hostPath.path }}
