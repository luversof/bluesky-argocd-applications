apiVersion: v1
kind: Secret
metadata:
  name: superset-secret
  labels:
    app: superset
type: Opaque
# Superset 은 세션/암호화에 SECRET_KEY 를 사용한다. 운영 전 아래 값을 랜덤 문자열로 교체할 것.
# (Python 불필요 — 아무 도구로나 생성 가능)
#   PowerShell : [Convert]::ToBase64String((1..42|%{Get-Random -Max 256}))
#   또는 openssl: openssl rand -base64 42
#   또는 그냥 40자 이상 아무 랜덤 문자열을 직접 입력해도 됨.
stringData:
  superset-secret-key: "hA2f6kFD4hu4giQOmDiQYMZYrrW2S1KPdPEFIfv+qt8rAIwC/wKSjC11"
