nameOverride: bluesky-web-gate

image:
  artifactId: bluesky-web-gate
  
service:
  port: 30122
  nodePort: 30122

ingress:
  enabled: true
  hosts:
  - host: swagger.web.bluesky.local
    paths:
    - path: /
      pathType: Prefix